# Boxing GLB assets

Drop future `.glb` files here. Loader refs use paths like `/boxing/<file>.glb`.

Until GLBs exist, the boxing loader uses typed box catalogs under `src/assets/boxing/boxes/` and falls back to those when a GLB ref includes `boxesFallbackId`.
